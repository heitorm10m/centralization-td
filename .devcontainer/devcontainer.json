{
  "name": "centraltd-python",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "ms-python.black-formatter"
      ]
    }
  },
  "postCreateCommand": "python3 -m pip install --upgrade pip && python3 -m pip install -e python[dev]",
  "remoteUser": "vscode"
}
