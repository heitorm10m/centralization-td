# Benchmarks

This directory is reserved for canonical benchmark cases that will be used to compare future solver phases.

## Current State

- Phase 1 ships only a minimal YAML-driven example in `examples/minimal_case.yaml`.
- No calibrated reference dataset is included yet.
- Future benchmark additions should document provenance, expected behavior, and numerical tolerances.

